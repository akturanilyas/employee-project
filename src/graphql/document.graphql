query Country {
    country {
        currency
    }
}

query Employee {
    employee {
        id
        name
        star
    }
}

mutation MutationTest($ep: String!, $review: String!, $star:Int!) {
    createEmployee(name: $ep, id: $review,star:$star ) {
        star
        id
    }
}
