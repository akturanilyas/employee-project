type Query {
    employee(id: Int!): Employee!
    employees: [Employee!]
}

type Mutation {
    increaseEmployeeStar(id: Int!): Employee!
}

type Employee {
    id: Int!
    first_name:String!
    last_name:String!
    email:String!
    image:String!
    job:String!
    star:Int!
}
